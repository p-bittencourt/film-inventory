<div class="create-page">
  <h1><%= director ? 'Edit Director' : 'Create Director' %></h1>
  <form id="createEditForm" method="post" onsubmit="handleFormSubmission(event, 'createEditForm')">
    <div>
      <label for="first_name">First Name:</label>
      <input
        type="text"
        id="first_name"
        name="first_name"
        required
        maxlength="100"
        value="<%= director ? director.first_name : '' %>"
      />
    </div>
    <div>
      <label for="family_name">Family Name:</label>
      <input
        type="text"
        id="family_name"
        name="family_name"
        maxlength="100"
        value="<%= director ? director.family_name : '' %>"
      />
    </div>
    <div>
      <label for="date_of_birth">Date of Birth:</label>
      <input
        type="date"
        id="date_of_birth"
        name="date_of_birth"
        value="<%= director ?
      director.date_of_birth : '' %>"
      />
    </div>
    <div>
      <label for="date_of_death">Date of Death:</label>
      <input
        type="date"
        id="date_of_death"
        name="date_of_death"
        value="<%= director ?
      director.date_of_death : '' %>"
      />
    </div>
    <div>
      <label for="nationality">Nationality:</label>
      <input
        type="text"
        id="nationality"
        name="nationality"
        value="<%= director ? director.nationality : '' %>"
      />
    </div>
    <div>
      <label for="picture">Picture URL:</label>
      <input
        type="url"
        id="picture"
        name="picture"
        value="<%= director ? director.picture : '' %>"
      />
    </div>
    <div>
      <label for="movies">Movies:</label>
      <select id="movies" name="movies" multiple>
        <% movies.forEach(function(movie) { %>
          <option value="<%= movie._id %>" <%= director && director.movies.includes(movie._id) ? 'selected' : '' %>><%= movie.title %></option>
        <% }); %>
      </select>
    </div>
    <div>
      <label for="password">Enter password:</label>
      <input type="password" name="password" id="password" required />
      <button type="submit"><%= director ? 'Update' : 'Create' %></button>
    </div>
  </form>
</div>